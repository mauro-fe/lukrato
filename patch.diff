diff --git a/views/admin/partials/header.php b/views/admin/partials/header.php
index a5a894a..d003ec6 100644
--- a/views/admin/partials/header.php
+++ b/views/admin/partials/header.php
@@ -70,10 +70,36 @@ $csrfToken = CsrfMiddleware::generateToken('default'); // MESMO ID do handle()
     </button>
 
     <aside class="sidebar no-glass collapsed" id="sidebar-main">
         <div class="sidebar-header">
             <a class="logo" href="<?= BASE_URL ?>/dashboard" aria-label="Ir para o Dashboard">
                 <img src="<?= BASE_URL ?>assets/img/logo.png" alt="Lukrato">
             </a>
         </div>
-        <nav class="sidebar-nav">
-            <button id="toggleTheme" type="button" class="nav-item theme-toggle mb-3" aria-label="Alternar tema"
-                title="Modo claro/escuro">
-                <i class="fas fa-sun"></i>
-                <i class="fas fa-moon"></i>
-            </button>
+        <nav class="sidebar-nav">
             <a href="<?= BASE_URL ?>dashboard" class="nav-item <?= $active('dashboard')   ?>"
                 <?= $aria('dashboard')   ?> title="Dashboard"><i class="fas fa-home"></i><span>Dashboard</span></a>
@@ -95,6 +121,54 @@ $csrfToken = CsrfMiddleware::generateToken('default'); // MESMO ID do handle()
 
 
 
         </div>
     </aside>
     <div id="sidebarBackdrop" class="sidebar-backdrop"></div>
 
     <main class="main-content">
+        <header class="lk-topbar d-flex align-items-center justify-content-between gap-3">
+            <div class="lk-topbar-title d-flex align-items-center gap-2">
+                <i class="fas fa-chart-line"></i>
+                <span><?= htmlspecialchars($pageTitle ?? 'Dashboard', ENT_QUOTES, 'UTF-8') ?></span>
+            </div>
+            <div class="d-flex align-items-center gap-2">
+                <button id="toggleTheme" type="button" class="btn btn-outline-light position-relative"
+                    aria-label="Alternar tema">
+                    <i class="fas fa-sun"></i>
+                </button>
+                <button id="lk-open-agendamento"
+                    class="btn btn-outline-light position-relative"
+                    type="button" aria-label="Agendar pagamento">
+                    <i class="fas fa-calendar-plus"></i>
+                </button>
+                <div class="position-relative">
+                    <button id="lk-bell" type="button" class="btn btn-outline-light position-relative"
+                        aria-label="Notificações">
+                        <i class="fas fa-bell"></i>
+                        <span id="lk-bell-badge" class="badge bg-danger position-absolute top-0 end-0 translate-middle"
+                            style="display:none">0</span>
+                    </button>
+                    <div id="lk-bell-menu" class="lk-popover hidden">
+                        <div class="lk-popover-card">
+                            <div class="lk-popover-h">Avisos</div>
+                            <div id="lk-bell-list" class="lk-popover-b"></div>
+                            <div class="lk-popover-f">
+                                <button id="lk-mark-read" class="lk-btn">Marcar como lidas</button>
+                            </div>
+                        </div>
+                    </div>
+                </div>
+            </div>
+        </header>
         <div class="container-fluid lk-page">
             <!-- Modal: Novo lancamento -->
             <div class="modal fade" id="modalLancamento" tabindex="-1" aria-labelledby="modalLancamentoTitle"
@@ -328,6 +402,32 @@ $csrfToken = CsrfMiddleware::generateToken('default'); // MESMO ID do handle()
                     const modal = new bootstrap.Modal(el);
                     modal.show();
                 });
+
+                document.getElementById('lk-open-agendamento')?.addEventListener('click', () => {
+                    const el = document.getElementById('modalAgendamento');
+                    if (!el) return;
+                    bootstrap.Modal.getOrCreateInstance(el).show();
+                });
             })();
         </script>
 
         <script>
             (function() {
@@ -360,7 +460,10 @@ $csrfToken = CsrfMiddleware::generateToken('default'); // MESMO ID do handle()
 
                     function updateIcon(theme) {
                         const btnEl = document.getElementById("toggleTheme");
                         if (!btnEl) return;
-                        btnEl.classList.toggle('dark', theme === 'dark');
+                        const icon = btnEl.querySelector('i.fas');
+                        if (!icon) return;
+                        icon.classList.remove('fa-sun', 'fa-moon');
+                        icon.classList.add(theme === 'dark' ? 'fa-moon' : 'fa-sun');
                     }
                 })();
             </script>
